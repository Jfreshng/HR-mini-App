# <VirtualHost *:10000>
#     ServerName localhost
#     DocumentRoot /var/www/html/public

#     <Directory /var/www/html/public>
#         Options +FollowSymLinks -Indexes
#         AllowOverride All
#         Require all granted

#         # Let Apache fall back to index.php for CI4 routing
#         FallbackResource /index.php
#     </Directory>

#     # Ensure other folders are accessible when needed
#     <Directory /var/www/html>
#         AllowOverride All
#         Require all granted
#     </Directory>

#     # Logs
#     ErrorLog ${APACHE_LOG_DIR}/error.log
#     CustomLog ${APACHE_LOG_DIR}/access.log combined
# </VirtualHost>


<VirtualHost *:10000>
    ServerName localhost
    DocumentRoot /var/www/html/public

    <Directory /var/www/html/public>
        Options +FollowSymLinks -Indexes
        AllowOverride All
        Require all granted
        FallbackResource /index.php
    </Directory>

    <Directory /var/www/html>
        AllowOverride All
        Require all granted
    </Directory>

    # Enable detailed PHP errors for debugging
    php_flag display_errors on
    php_value error_reporting E_ALL
    php_flag log_errors on

    ErrorLog ${APACHE_LOG_DIR}/error.log
    CustomLog ${APACHE_LOG_DIR}/access.log combined
</VirtualHost>